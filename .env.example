# Copy to .env. Full MagicDNS name of this machine (`tailscale status --json | jq .Self.DNSName`,
# without the trailing dot). HTTPS Certificates must be enabled in the Tailscale admin console (DNS page).
TS_DOMAIN=host.tailnet-name.ts.net
